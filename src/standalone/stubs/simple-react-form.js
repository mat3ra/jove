/**
 * Minimal stub for simple-react-form used in MapDataForm.tsx.
 * The standalone Vite app does not render MapDataForm, so this stub
 * only needs to satisfy the import without throwing.
 */
export const Field = () => null;
export const Form = ({ children }) => children ?? null;
export default { Field, Form };
